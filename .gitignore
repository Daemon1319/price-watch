# Editor / IDE (local only)
.vscode/
.idea/
*.iml

# OS
.DS_Store
Thumbs.db
desktop.ini

# Env / secrets at monorepo root
.env
.env.*

# Empty leftover nest from monorepo hoist (delete locally when no process locks it)
/price_watch/
